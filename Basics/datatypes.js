"use strict"; //treat all javascript code as newer version code .

//alert(3 + 3); //alert can be use like this only on browser not in node environment
//code should be readable -> code readability should be high

// Datatype in javascript

//number => 2 to the power 53
// bigint //probably will never use
//string => '' anyting inside single quote or double quote or inside string letrals
// null => also a standalonevalue or also a datatype
//undefine => value hasnot been assigned.
//symbol => for unique
//object

console.log(typeof "roshan");
console.log(typeof null);
console.log(typeof undefined);
